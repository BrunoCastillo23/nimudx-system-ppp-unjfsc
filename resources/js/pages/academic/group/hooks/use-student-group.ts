/**
 * @deprecated Archivo huérfano: quedó vacío (sin exports) y ningún otro archivo
 * del proyecto lo importa. La gestión de estudiantes por grupo la implementa
 * `use-student-management.ts` (en este mismo directorio `hooks/`), usado por
 * `pages/academic/group/student/partials/student-management-tabs.tsx`.
 * Este archivo es seguro de eliminar manualmente; se deja este comentario
 * porque las herramientas disponibles no permiten borrar archivos.
 */
